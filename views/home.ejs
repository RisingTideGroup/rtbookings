<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Welcome to the Booking Page</title>
  <style>
    :root {
      --brand-color: <%= brandColor %>;
      --text-color: <%= textColor %>;
      --button-bg-color: <%= buttonBgColor %>;
      --button-text-color: <%= buttonTextColor %>;
      --button-hover-bg-color: <%= buttonHoverBgColor %>;
      --button-hover-text-color: <%= buttonHoverTextColor %>;
    }
  </style>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <!-- Include header -->
  <%- include('partials/header', { brandColor, logoUrl, textColor }) %>

  <!-- Page Content -->
  <div style="padding: 20px;">
    <h1>Welcome to Our Booking Page</h1>
    <p>Thank you for visiting. Please navigate to your specific agent's link to book an appointment.</p>
  </div>

  <!-- Include footer -->
  <%- include('partials/footer', { brandColor, textColor, Org }) %>
</body>
</html>
